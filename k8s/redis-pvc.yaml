apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: redis-pvc
    namespace: nestjs-starter
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
    storageClassName: standard
